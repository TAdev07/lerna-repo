{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test": "lerna run test --scope={@lerna-repo/logger,@lerna-repo/api}",
    "start": "lerna run start"
  }
}
